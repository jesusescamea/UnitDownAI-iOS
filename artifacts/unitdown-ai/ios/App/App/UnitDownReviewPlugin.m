/**
 * UnitDownReviewPlugin.m
 *
 * Intentionally minimal. See UnitDownIAPPlugin.m for full explanation.
 *
 * UnitDownReviewPlugin is registered explicitly in ViewController.swift via
 * bridge?.registerPluginInstance(UnitDownReviewPlugin()) — not via the
 * CAP_PLUGIN macro, which only works for npm-distributed plugins.
 */
